= form_for(@news_item) do |f|
  .field= f.select :kind, NewsItem::KINDS
  .field= f.text_field :headline, :placeholder => 'Enter a headline'
  .field= f.text_area :content, :class => 'editor', :placeholder => 'Write the content of the news item'
  = f.submit 'Post'
