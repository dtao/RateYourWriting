%header
  %h1=h @submission.title
  .author= link_to(@submission.user.name, @submission.user)

%article.submission
  != preserve(submission_body(@submission))

- if !logged_in?
  %p= "You must #{link_to('log in', login_path)} to vote!"
- else
  = voting_section
