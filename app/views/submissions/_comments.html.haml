%section.comments
  %legend Comments
  - @comments.each do |comment|
    .comment
      .commenter= link_to(comment.user.name, comment.user)
      .time= "#{time_ago_in_words(comment.created_at)} ago"
      .content!= to_markdown(comment.content)
